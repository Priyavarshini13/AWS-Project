# AWS-Project
This project demonstrates the creation of a cloud instance and running a server using its public IP address. The instance was created on AWS EC2 using Ubuntu, and it can be accessed remotely using SSH for deploying and managing applications.
